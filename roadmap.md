# Roadmap

## План рефакторинга архитектуры плеера

### 1. Переход на Zustand [ВЫПОЛНЕНО]
- [x] Замена `PlayerContext` на `zustand`.
- [x] Создание независимого хранилища и хука `usePlayerStore`.

### 2. Реорганизация файлов и компонентов [ВЫПОЛНЕНО]
- [x] Миграция кода в `src/`.
- [x] Декомпозиция монолита плеера на автономные папки (`PlayerControls`, `MobilePlayer`, `LyricsDrawer`).
- [x] Внедрение персональных CSS модулей для каждого компонента.

### 3. Независимая загрузка метаданных и кеширование [ВЫПОЛНЕНО]
- [x] Выделение извлечения цветов в хук `useTrackColor` с кешем.
- [x] Обеспечение независимости Lyrics от загрузки аудиоконтекста.
- [x] Исправление логики скролла в текстах.

### 4. Улучшение работы Media Session API [ВЫПОЛНЕНО]
- [x] Устранение конфликтов Web Audio API с системным уведомлением.
- [x] Отказ от использования `dummyAudio` и `setPositionState` (решение проблемы "исчезающей паузы").
- [x] Обеспечение стабильной работы сессии на мобильных устройствах без ущерба для визуализации.

### 5. Регулятор громкости [ВЫПОЛНЕНО]
- [x] Внедрение настройки уровня громкости в стор и UI.
- [x] Синхронизация громкости с `ReactHowler`.

### 6. Оптимизация производительности [ВЫПОЛНЕНО]
- [x] Переход на атомарные селекторы Zustand.
- [x] Изоляция компонентов с высокой частотой обновлений (прогресс-бар, таймеры).

### 7. Глобальная стабилизация и кастомный аудио-движок [ВЫПОЛНЕНО]
- [x] Полный отказ от устаревшей библиотеки `react-howler`.
- [x] Перенос инстанцирования `Howl` в Zustand Actions (решение проблем Autoplay).
- [x] Реализация синхронного уничтожения (`unload`) старых нод для очистки памяти.
- [x] Исправление визуализатора `AudioMotion` под новую архитектуру без задержек.
- [x] Фикс `setPositionState` спама в Media Session (стабильность на Android).
- [x] Исправление типов и пропсов UI компонентов (`Button` variants).

### 8. Технологическое обновление стека [ВЫПОЛНЕНО]
- [x] Миграция на **Next.js 16** и **React 19**.
- [x] Внедрение **Tailwind CSS v4** (миграция конфигов в CSS).
- [x] Обновление **Shadcn UI до v4** (переход на `radix-ui`).
- [x] Миграция на **pnpm**.
- [x] Перевод ESLint на **Flat Config**.
- [x] Настройка **Turbopack** с поддержкой SVGR.

---

## Миграция на удалённую базу данных [ВЫПОЛНЕНО]

### 9. Prisma ORM и MySQL [ВЫПОЛНЕНО]
- [x] Установка и настройка Prisma ORM v6 (`prisma-client-js` генератор).
- [x] Проектирование схемы БД (User, Track, Playlist, PlaylistTrack, Favorites, Better Auth таблицы).
- [x] Успешное подключение к удалённому MySQL на shared-хостинге (108.165.32.191).
- [x] Генерация миграций и синхронизация схемы.

### 10. Миграция данных [ВЫПОЛНЕНО]
- [x] Перенос 320 треков из `data.ts` в таблицу `Track`.
- [x] Перенос 25 системных плейлистов в таблицу `Playlist` с привязками `PlaylistTrack`.
- [x] Автосоздание inline-треков из плейлиста "stud." при миграции.

### 11. Аутентификация Better Auth [ВЫПОЛНЕНО]
- [x] Настройка серверной части (`src/lib/auth.ts`) с `prismaAdapter` и плагином `admin`.
- [x] Настройка клиентской части (`src/lib/auth-client.ts`) с `adminClient`.
- [x] Route handler (`/api/auth/[...all]`).
- [x] Хуки отслеживания входов (`loginCount`, `lastLoginAt`).

### 12. Audio Streaming API [ВЫПОЛНЕНО]
- [x] Route Handler `/api/stream/[id]` с Range Requests (206 Partial Content).
- [x] **Оптимизация кэширования**: Внедрение `Cache-Control`, `ETag` и `304 Not Modified` для мгновенной загрузки.
- [x] Утилита `id3.ts` для чтения/записи метаданных MP3.

---

## Следующие этапы (TODO)

### 13. Админ-панель (`/admin`) [ВЫПОЛНЕНО]
- [x] Рефакторинг на **shadcn/ui** и **animate-ui** (полный отказ от HeroUI).
- [x] Создать Layout `src/app/admin/layout.tsx` с проверкой роли `ADMIN`.
- [x] **Управление треками**: Таблица всех треков (TanStack Table v8), поиск, пагинация, модальные окна редактирования.
- [x] **Управление плейлистами**: Таблица коллекций, массовое удаление, редактирование категорий и приватности.
- [x] **Управление пользователями**: Таблица участников, бан/разбан, смена ролей (ADMIN/USER).
- [x] Устранение ошибок гидратации и оптимизация Admin API роутов.

### 14. Страницы аутентификации [В РАБОТЕ]
- [ ] Страница `/sign-in` (вход) — перенос на shadcn.
- [ ] Страница `/sign-up` (регистрация) — перенос на shadcn.
- [ ] Кнопка/логика Sign Out в интерфейсе.

### 15. Клиент: Медиатека, Очередь и Стриминг [ВЫПОЛНЕНО]
- [x] Перевод PlayerStore на `/api/stream/{id}`.
- [x] Редактирование очереди: **Drag-and-Drop** (`@hello-pangea/dnd`) и удаление треков.
- [x] Синхронизация очереди с базой данных (JSON field).
- [x] **Статистика и история**
    - [x] Сохранение `TrackHistory` при завершении трека.
    - [x] Страница истории последних прослушиваний.
- [x] **Избранное (Лайки)**
    - [x] Универсальная кнопка `LikeButton`.
    - [x] Раздел "Liked Songs" в медиатеке.
- [x] **Управление очередью (Advanced)**
    - [x] Персистентная очередь в БД.
    - [x] **Исправление Fallback**: Очередь больше не забивается всеми треками системы при старте.
    - [x] Перетаскивание (Drag-and-Drop) треков в очереди.
    - [x] Удаление/очистка очереди.
- [x] **Синхронизация состояния**
    - [x] Унифицированная синхронизация всей очереди, активного трека и плейлиста в базе данных (User.queue + lastPlayed IDs).
    - [x] Снижение дебаунса синхронизации до 2 секунд и немедленное сохранение при старте нового трека.
    - [x] Использование `localStorage` как высокоскоростного вторичного кэша для мгновенной гидратации UI.
    - [x] Исправлено восстановление Vibe-плейлистов спустя сессии.

### 16. Будущие улучшения (TODO)
- [ ] **HLS/DASH Стриминг**: Архитектура сегментации и адаптивный битрейт.
- [x] **Админка: Массовая загрузка**: Многошаговый интерфейс (`MultiStepUploadSheet`) с очередью файлов и предпросмотром метаданных.
- [x] **Drag-and-Drop в плейлистах**: Интерфейс изменения порядка треков в `/admin/playlists` с поддержкой числовых ID.
- [x] **Масштабная миграция ID (String → Int)**: Полный переход всей системы на целочисленные идентификаторы треков с сохранением данных. [ВЫПОЛНЕНО]
- [x] **AI-Генерация обложек**: Интеграция с агентом (`generate_image`) для создания уникального стиля плейлистов.
- [x] **Мульти-загрузка**: Пакетная загрузка треков в админку с автоматическим парсингом ID3 и извлечением цветовой палитры.
- [x] **Управление обложками плейлистов**: Загрузка локальных файлов и экстракция цветов. [ВЫПОЛНЕНО]
- [ ] **Пользовательские плейлисты**: Возможность создавать свои коллекции («Мне нравится» уже работает как база).

### 17. Обновление UI: Анимированное управление [ВЫПОЛНЕНО]
- [x] **Анимированное появление и Skeleton Loading**:
    - [x] Внедрена "spring" анимация появления плеера снизу (`slideUp`).
    - [x] Реализовано состояние Skeleton Loading (мерцающий макет) для моментального отклика до загрузки данных из БД.
    - [x] Добавлен эффект мягкого проявления (`fadeIn`) контента плеера при готовности данных.
- [x] Реализация SVG-морфинга для кнопки Play/Pause (чистый CSS).
- [x] Внедрение "spring" анимации вращения и micro-interactions.
- [x] Декомпозиция управления на `PlayButton`, `StepButton` и `PlaybackButtons`.
- [x] Динамическая адаптация цветов кнопок под обложку трека.
- [x] Интеграция нового UI во все состояния плеера (Desktop, Mobile Mini, Expanded).
- [x] Оптимизация CSS Grid для консолидированного управления.
- [x] **Carousel Swipe UX**: Direction locking (горизонтальный свайп для треков, вертикальный для закрытия).

### 18. Гибридная система экстракции цветов [ВЫПОЛНЕНО]
- [x] Интеграция `fast-average-color-node` (алгоритмы Dominant, Sqrt, Simple).
- [x] Нормализация изображений через `sharp` (фикс цветовых искажений в ЧБ).
- [x] Архитектура "Absolute Cache": приоритет БД над повторным анализом.
- [x] Флаг `force` для админов для принудительного обновления палитры.
- [x] Синхронизация цветов через `playerStore` для устранения мерцания UI.
### 19. Глобальная аудио-реактивность и Audio Lab [ВЫПОЛНЕНО]
- [x] **Унификация AudioMotion**: Переход на синглтон-архитектуру анализатора (`sharedAnalyzer`).
- [x] **Zustand Audio Store**: Реализация real-time стора для частотных полос и BPM.
- [x] **useAudioReactiveMotion**: Разработка 60fps хука для Framer Motion без ре-рендеров.
- [x] **Система усиления (Boost/Ultra)**: Трехуровневая нормализация частот для разной чувствительности UI.
- [x] **Living Background (Mobile)**:
    - [x] Реактивное масштабирование обложки от Treble.
    - [x] Реактивное управление фильтрами (brightness/saturate) от Bass.
    - [x] Использование `useMotionTemplate` для оптимизации фильтров.
- [x] **Audio Reactive Lab**:
    - [x] Разработка физики вращения винила для `BPMRotator`.
    - [x] Создание набора реактивных компонентов (`BassPulse`, `MidShift`, `TrebleGlow`, `OmniVibeCube`).
    - [x] Интеграция лаборатории на главную страницу.
- [x] **Page Color API**:
    - [x] Разработка хука `usePageVisualizerColors` для кастомных тем страниц.
    - [x] Реализация автоматического сброса цвета при размонтировании страницы.
    - [x] Тематизация страницы `/about`.
- [x] **Оптимизация производительности**: Синхронизация обновлений стора с частотой обновления экрана (60fps).
- [x] **Carousel 2.0 (Mobile)**:
    - [x] Реализован карусельный стрип с 3-мя активными слотами и динамической интерполяцией стилей (`transform`, `opacity`).
    - [x] Синхронизировано позиционирование плеера и навигационной панели через единый якорь `bottom: 0`.
    - [x] Исправлена ошибка рекурсии (`Maximum update depth`) за счет стабилизации слушателетей изменения размера.
    - [x] Добавлена поддержка безопасных зон `env(safe-area-inset-bottom)` для мобильных устройств.